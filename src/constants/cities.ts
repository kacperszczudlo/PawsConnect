/** Filter / profile value meaning “no city filter”. */
export const ALL_POLAND_CITY_LABEL = 'Cała Polska' as const;

/**
 * Photon bbox: minLon, minLat, maxLon, maxLat — approximate Poland.
 * @see https://photon.komoot.io
 */
export const POLAND_PHOTON_BBOX = [14.07, 49.0, 24.15, 54.85] as const;
